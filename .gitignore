# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules/
dist/
dist-ssr/
*.local

# Tauri build output
src-tauri/target/

# Secrets
.env
.env.local

# Generated MCP config (per-engagement, written at spawn time)
.mcp-config.json
.mcp-config.json.tmp

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Superpowers skill scratch (brainstorm sessions, etc.)
.superpowers/

# Phase E heartbeat (Python on-VM service)
heartbeat/.venv/
heartbeat/.pytest_cache/
heartbeat/.mypy_cache/
heartbeat/.ruff_cache/
heartbeat/**/__pycache__/
heartbeat/**/*.egg-info/
heartbeat/build/
heartbeat/dist/
# Operator secrets — never commit. Live in /etc/ikrs-heartbeat/secrets.env
# in production; locally they live alongside the example.
heartbeat/config/secrets.env
heartbeat/config/heartbeat.toml
